# Legacy Shelf 样本

这个目录承载历史置物架参考样本生成的文档与证据。

它的定位是：

- 保留早期“结构 -> 穷举 -> 验证 -> 可视化”的完整示范
- 作为仓库方法论来源之一
- 不再代表当前仓库默认主线

当前仓库默认主线见 [architecture.md](/home/xue/code/shelf/docs/architecture.md#L1)。

legacy 样本运行方式：

```bash
uv run python src/main.py legacy-reference-shelf
```
